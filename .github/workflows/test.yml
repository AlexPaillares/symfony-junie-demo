name: Test Workflow

on:
  push:
    branches:
      - master  # change if your default branch is not main
  workflow_dispatch: # allows manual trigger in GitHub UI

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Display repository files
        run: ls -al
